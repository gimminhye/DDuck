public class Main {
	public static void main(String[] args) {
		MyFrame f = new MyFrame();
		/*Frame f = new Frame(); //메모리에 프레임 객체를 올리고, window창에 필요한 모든 기능을 불러올 수있음
		f.setSize(800, 600); //프레임의 사이즈 기본값은 0,0이라 지정해줘야함
		Color bgColor = new Color(255, 0, 0); //컬러클래스는 디폴트 생성자가 없어서  rgb컬러를 입력해줘야함.
		f.setBackground(bgColor);
		f.setVisible(true); //프레임 내 멤버 객체 내 보이게 모니터를 쓰는기능 . 운영체제로부터 제어권을 넘겨받음 .이 이후에 그림을 그리던지 해야함.
		
		*/
	}
}
